---
- name: linux-utils | ensure /opt directory is owned by current user
    file:
        path: /opt
        owner: {{ user_id }}
        group: {{ user_group }}
        mode: 0755

- name: linux-utils | install vim latest
    apt:
        state: present
        name: vim
    become: true
    become_method: sudo
