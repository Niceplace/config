---
  - name: install rvm from custom script
    script: install_rvm.sh
    # From Ansible documentation
    #  Any copy, script, template or include tasks (in the role) can reference
    #  files in roles/x/{files,templates,tasks}/ (dir depends on task)
    #  without having to path them relatively or absolutely
